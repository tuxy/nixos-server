# TODO: replace with server02's SSH host ed25519 public key
#
# Get it after first deploy:
#   ssh-keyscan server02 | grep ed25519 | awk '{print $2, $3}' > secrets/pubkeys/server02.pub
#
# Or copy from the running server:
#   ssh root@server02 cat /etc/ssh/ssh_host_ed25519_key.pub > secrets/pubkeys/server02.pub
